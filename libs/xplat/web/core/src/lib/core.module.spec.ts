describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { BazCoreModule } from './core.module';

describe('BazCoreModule', () => {
  it('should work', () => {
    expect(new BazCoreModule(null)).toBeDefined();
  });
});
*/
